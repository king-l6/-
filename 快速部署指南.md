# 快速部署指南

本文档提供最简单的部署方法，适合快速上线。

## 🚀 方式一：Docker 一键部署（推荐，最简单）

### 前置要求
- 已安装 Docker 和 Docker Compose

### 部署步骤

```bash
# 1. 进入项目目录
cd 量化

# 2. 一键部署
docker-compose up -d

# 3. 查看日志（确认服务正常）
docker-compose logs -f

# 4. 访问应用
# 浏览器打开: http://localhost:8086
```

### 停止服务

```bash
docker-compose down
```

### 更新部署

```bash
git pull
docker-compose up -d --build
```

---

## 🖥️ 方式二：传统服务器部署

### 前置要求
- Python 3.11+
- Node.js 18+

### 部署步骤

```bash
# 1. 使用部署脚本（推荐）
./deploy.sh traditional

# 2. 手动部署步骤：

# 2.1 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 2.2 安装依赖
pip install -r requirements-prod.txt

# 2.3 构建前端
cd frontend
npm install
npm run build
cd ..

# 2.4 复制前端文件到 static 目录
cp -r frontend/dist/* static/

# 2.5 启动服务（生产环境）
gunicorn -w 4 -b 0.0.0.0:8086 --timeout 600 app:app
```

### 使用 systemd 管理服务

创建 `/etc/systemd/system/stock-backtest.service`：

```ini
[Unit]
Description=Stock Backtest Application
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/path/to/量化
Environment="PATH=/path/to/量化/venv/bin"
ExecStart=/path/to/量化/venv/bin/gunicorn -w 4 -b 0.0.0.0:8086 --timeout 600 app:app
Restart=always

[Install]
WantedBy=multi-user.target
```

启动服务：

```bash
sudo systemctl daemon-reload
sudo systemctl enable stock-backtest
sudo systemctl start stock-backtest
```

---

## ☁️ 方式三：云服务器部署（阿里云/腾讯云）

### 1. 购买服务器
- 推荐配置：2核4G，Ubuntu 22.04
- 开放端口：8086（HTTP），443（HTTPS，可选）

### 2. SSH 连接服务器

```bash
ssh root@your-server-ip
```

### 3. 安装 Docker（如果使用 Docker 方式）

```bash
curl -fsSL https://get.docker.com | bash
systemctl start docker
systemctl enable docker
```

### 4. 部署应用

```bash
# 克隆项目
git clone <your-repo-url>
cd 量化

# 使用 Docker 部署
docker-compose up -d
```

### 5. 配置域名和 HTTPS（可选）

参考 `部署文档.md` 中的 Nginx 配置部分。

---

## 🔧 常见问题

### Q: 端口被占用怎么办？
A: 修改 `docker-compose.yml` 中的端口映射，或修改环境变量 `FLASK_PORT`

### Q: 如何查看日志？
A: 
- Docker: `docker-compose logs -f`
- 传统方式: 查看 systemd 日志 `journalctl -u stock-backtest -f`

### Q: 前端无法访问后端？
A: 检查防火墙规则，确保端口已开放

### Q: 回测超时？
A: 增加超时时间配置，参考 `部署文档.md`

---

## 📚 更多信息

详细部署文档请参考：`部署文档.md`
