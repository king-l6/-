# 多实例运行说明

可以在不同的终端同时运行多个前端和后端实例，以便同时回测不同的策略。

## 快速开始

### 方案1：使用预配置的脚本（推荐）

#### 实例1（默认端口）
**终端1 - 后端：**
```bash
cd /Users/bilibili/Desktop/test/量化
source venv/bin/activate  # 如果使用虚拟环境
python app.py
# 后端运行在 http://localhost:8086
```

**终端2 - 前端：**
```bash
cd /Users/bilibili/Desktop/test/量化/frontend
npm run dev:5173
# 前端运行在 http://localhost:5173，连接后端 8086
```

#### 实例2（端口 5174/8087）
**终端3 - 后端：**
```bash
cd /Users/bilibili/Desktop/test/量化
source venv/bin/activate  # 如果使用虚拟环境
FLASK_PORT=8087 python app.py
# 后端运行在 http://localhost:8087
```

**终端4 - 前端：**
```bash
cd /Users/bilibili/Desktop/test/量化/frontend
npm run dev:5174
# 前端运行在 http://localhost:5174，连接后端 8087
```

#### 实例3（端口 5175/8088）
**终端5 - 后端：**
```bash
cd /Users/bilibili/Desktop/test/量化
source venv/bin/activate  # 如果使用虚拟环境
FLASK_PORT=8088 python app.py
# 后端运行在 http://localhost:8088
```

**终端6 - 前端：**
```bash
cd /Users/bilibili/Desktop/test/量化/frontend
npm run dev:5175
# 前端运行在 http://localhost:5175，连接后端 8088
```

### 方案2：自定义端口

#### 后端自定义端口
```bash
FLASK_PORT=8089 python app.py
```

#### 前端自定义端口和代理地址
```bash
# 设置前端端口和后端地址
VITE_PORT=5180 VITE_API_TARGET=http://localhost:8089 npm run dev
```

## 端口分配建议

| 实例 | 前端端口 | 后端端口 | 说明 |
|------|---------|---------|------|
| 实例1 | 5173 | 8086 | 默认实例 |
| 实例2 | 5174 | 8087 | 第二个实例 |
| 实例3 | 5175 | 8088 | 第三个实例 |
| 实例N | 5173+N | 8086+N | 第N个实例 |

## 注意事项

1. **每个后端实例独立运行**：每个后端实例都有独立的数据缓存和策略缓存
2. **数据共享**：所有实例共享 `cache/` 目录下的数据缓存文件，但不会冲突
3. **结果文件**：每个实例的回测结果会保存在 `results/` 目录，文件名包含时间戳，不会冲突
4. **资源占用**：每个后端实例会占用一定的内存和 CPU，建议根据机器性能决定运行几个实例
5. **并发限制**：每个后端实例默认使用 50 个并发线程，多个实例会共享网络资源

## 示例：同时回测3个策略

```bash
# 终端1：实例1 - 策略A
cd /Users/bilibili/Desktop/test/量化
python app.py

# 终端2：前端1
cd /Users/bilibili/Desktop/test/量化/frontend
npm run dev:5173

# 终端3：实例2 - 策略B
cd /Users/bilibili/Desktop/test/量化
FLASK_PORT=8087 python app.py

# 终端4：前端2
cd /Users/bilibili/Desktop/test/量化/frontend
npm run dev:5174

# 终端5：实例3 - 策略C
cd /Users/bilibili/Desktop/test/量化
FLASK_PORT=8088 python app.py

# 终端6：前端3
cd /Users/bilibili/Desktop/test/量化/frontend
npm run dev:5175
```

然后在浏览器中打开：
- http://localhost:5173 - 回测策略A
- http://localhost:5174 - 回测策略B
- http://localhost:5175 - 回测策略C

## 故障排查

1. **端口被占用**：如果端口被占用，可以更换其他端口
2. **CORS 错误**：后端已配置 CORS，应该不会有跨域问题
3. **数据冲突**：虽然数据缓存共享，但每个实例的请求是独立的，不会冲突
