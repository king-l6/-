# 项目部署文档

本文档提供多种部署方案，适用于不同的部署场景。

## 目录

1. [Docker 部署（推荐）](#docker-部署推荐)
2. [传统服务器部署](#传统服务器部署)
3. [云平台部署](#云平台部署)
4. [Nginx 反向代理配置](#nginx-反向代理配置)
5. [环境变量配置](#环境变量配置)
6. [常见问题](#常见问题)

---

## Docker 部署（推荐）

### 前置要求

- Docker 20.10+
- Docker Compose 2.0+

### 快速开始

1. **构建并启动服务**

```bash
docker-compose up -d
```

2. **查看日志**

```bash
docker-compose logs -f
```

3. **停止服务**

```bash
docker-compose down
```

4. **访问应用**

打开浏览器访问：http://localhost:8086

### 自定义配置

编辑 `docker-compose.yml` 文件，可以修改：
- 端口映射（默认 8086:8086）
- 环境变量
- 数据卷挂载路径

---

## 传统服务器部署

### 方案一：前后端分离部署

#### 1. 服务器要求

- Python 3.11+
- Node.js 18+
- Nginx（用于反向代理和静态文件服务）

#### 2. 后端部署

```bash
# 1. 克隆项目
git clone <your-repo-url>
cd 量化

# 2. 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 3. 安装依赖
pip install -r requirements.txt

# 4. 使用 Gunicorn 运行（生产环境推荐）
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:8086 --timeout 600 app:app
```

#### 3. 前端部署

```bash
# 1. 进入前端目录
cd frontend

# 2. 安装依赖
npm install

# 3. 构建生产版本
npm run build

# 4. 构建产物在 frontend/dist 目录
```

#### 4. Nginx 配置

创建 `/etc/nginx/sites-available/stock-backtest`：

```nginx
server {
    listen 80;
    server_name your-domain.com;  # 替换为你的域名

    # 前端静态文件
    root /path/to/量化/frontend/dist;
    index index.html;

    # 前端路由（Vue Router）
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 后端 API 代理
    location /api {
        proxy_pass http://127.0.0.1:8086;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 增加超时时间（回测可能需要较长时间）
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

启用配置：

```bash
sudo ln -s /etc/nginx/sites-available/stock-backtest /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### 方案二：单机部署（前后端一起）

如果不需要 Nginx，可以直接运行 Flask 并配置静态文件：

1. **修改 app.py**，添加静态文件服务：

```python
# 在 app.py 中添加
app = Flask(__name__, static_folder='frontend/dist', static_url_path='')
CORS(app)

@app.route('/')
def index():
    return app.send_static_file('index.html')

# 其他路由保持不变
```

2. **构建前端并复制到 Flask 静态目录**：

```bash
cd frontend
npm run build
cp -r dist/* ../static/
```

3. **运行 Flask**：

```bash
# 开发环境
python app.py

# 生产环境（使用 Gunicorn）
gunicorn -w 4 -b 0.0.0.0:8086 --timeout 600 app:app
```

---

## 云平台部署

### 阿里云 ECS

1. **购买 ECS 实例**（推荐配置：2核4G，Ubuntu 22.04）

2. **SSH 连接服务器**

```bash
ssh root@your-server-ip
```

3. **安装 Docker**

```bash
curl -fsSL https://get.docker.com | bash
systemctl start docker
systemctl enable docker
```

4. **部署应用**

```bash
# 克隆项目
git clone <your-repo-url>
cd 量化

# 使用 Docker Compose 部署
docker-compose up -d
```

5. **配置安全组**

在阿里云控制台配置安全组规则：
- 开放端口 8086（HTTP）
- 如需 HTTPS，开放端口 443

### 腾讯云 CVM

步骤与阿里云 ECS 类似，主要区别：
- 使用腾讯云 CVM 实例
- 在腾讯云控制台配置安全组

### 使用云容器服务（推荐）

#### 阿里云 ACK / 腾讯云 TKE

1. **构建镜像并推送到镜像仓库**

```bash
# 登录镜像仓库
docker login --username=your-username registry.cn-hangzhou.aliyuncs.com

# 构建镜像
docker build -t registry.cn-hangzhou.aliyuncs.com/your-namespace/stock-backtest:latest .

# 推送镜像
docker push registry.cn-hangzhou.aliyuncs.com/your-namespace/stock-backtest:latest
```

2. **在容器服务中创建应用**

- 选择镜像：使用上面推送的镜像
- 配置端口：8086
- 配置持久化存储：挂载 cache 和 results 目录

### 使用 Serverless（低成本方案）

#### 阿里云函数计算 FC / 腾讯云 SCF

注意：由于本项目需要持久化存储和长时间运行，Serverless 方案可能不太适合。如果必须使用，需要：
- 将数据存储迁移到 OSS/COS
- 使用异步任务处理回测

---

## Nginx 反向代理配置

### 完整配置示例（包含 HTTPS）

```nginx
# HTTP 重定向到 HTTPS
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$server_name$request_uri;
}

# HTTPS 配置
server {
    listen 443 ssl http2;
    server_name your-domain.com;

    # SSL 证书配置（使用 Let's Encrypt）
    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    # 前端静态文件
    root /path/to/量化/frontend/dist;
    index index.html;

    # 前端路由
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 后端 API 代理
    location /api {
        proxy_pass http://127.0.0.1:8086;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 超时配置
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
        
        # 支持 WebSocket（如果需要）
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/json application/javascript application/xml+rss;
}
```

### 使用 Let's Encrypt 免费 SSL 证书

```bash
# 安装 Certbot
sudo apt-get update
sudo apt-get install certbot python3-certbot-nginx

# 获取证书
sudo certbot --nginx -d your-domain.com

# 自动续期（已自动配置）
sudo certbot renew --dry-run
```

---

## 环境变量配置

### 后端环境变量

创建 `.env` 文件：

```bash
# Flask 配置
FLASK_ENV=production
FLASK_PORT=8086

# 数据源配置（如果需要）
AKSHARE_PROXY=
```

### 前端环境变量

在 `frontend/.env.production` 中配置：

```bash
# 生产环境 API 地址
VITE_API_TARGET=https://your-domain.com
```

然后重新构建前端：

```bash
cd frontend
npm run build
```

---

## 进程管理（生产环境）

### 使用 systemd（Linux）

创建 `/etc/systemd/system/stock-backtest.service`：

```ini
[Unit]
Description=Stock Backtest Application
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/path/to/量化
Environment="PATH=/path/to/量化/venv/bin"
ExecStart=/path/to/量化/venv/bin/gunicorn -w 4 -b 0.0.0.0:8086 --timeout 600 app:app
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

启动服务：

```bash
sudo systemctl daemon-reload
sudo systemctl enable stock-backtest
sudo systemctl start stock-backtest
sudo systemctl status stock-backtest
```

### 使用 Supervisor

安装 Supervisor：

```bash
sudo apt-get install supervisor
```

创建配置文件 `/etc/supervisor/conf.d/stock-backtest.conf`：

```ini
[program:stock-backtest]
command=/path/to/量化/venv/bin/gunicorn -w 4 -b 0.0.0.0:8086 --timeout 600 app:app
directory=/path/to/量化
user=www-data
autostart=true
autorestart=true
redirect_stderr=true
stdout_logfile=/var/log/stock-backtest.log
```

启动：

```bash
sudo supervisorctl reread
sudo supervisorctl update
sudo supervisorctl start stock-backtest
```

---

## 常见问题

### 1. 端口被占用

```bash
# 查看端口占用
lsof -i :8086
# 或
netstat -tulpn | grep 8086

# 修改端口（在 docker-compose.yml 或环境变量中）
```

### 2. 前端无法访问后端 API

- 检查 Nginx 配置中的 `proxy_pass` 地址
- 检查后端服务是否正常运行
- 检查防火墙规则

### 3. 回测超时

- 增加 Nginx 的 `proxy_read_timeout`
- 增加 Gunicorn 的 `--timeout` 参数
- 检查服务器资源（CPU、内存）

### 4. 数据获取失败

- 检查网络连接
- 检查防火墙是否允许出站请求
- 检查 akshare 数据源是否可用

### 5. Docker 构建失败

- 检查 Dockerfile 中的路径是否正确
- 检查依赖是否完整
- 查看构建日志：`docker-compose build --no-cache`

### 6. 静态文件 404

- 检查前端构建是否成功
- 检查 Nginx 配置中的 `root` 路径
- 检查文件权限

---

## 性能优化建议

1. **使用 CDN**：将前端静态资源部署到 CDN
2. **启用缓存**：配置 Redis 缓存回测结果
3. **数据库优化**：如果数据量大，考虑使用数据库存储股票数据
4. **负载均衡**：多实例部署时使用 Nginx 负载均衡
5. **监控告警**：使用 Prometheus + Grafana 监控应用状态

---

## 安全建议

1. **使用 HTTPS**：生产环境必须启用 HTTPS
2. **防火墙配置**：只开放必要端口
3. **定期更新**：及时更新依赖包
4. **日志管理**：配置日志轮转，避免磁盘占满
5. **备份数据**：定期备份 cache 和 results 目录

---

## 更新部署

### Docker 方式

```bash
# 拉取最新代码
git pull

# 重新构建并启动
docker-compose up -d --build
```

### 传统方式

```bash
# 拉取最新代码
git pull

# 更新依赖
pip install -r requirements.txt
cd frontend && npm install && npm run build

# 重启服务
sudo systemctl restart stock-backtest
# 或
sudo supervisorctl restart stock-backtest
```

---

## 联系支持

如有问题，请查看项目 README 或提交 Issue。
