# 定时任务说明

系统支持每天下午 3:30 自动执行更新数据和回测任务。

## 安装方式

### 方式一：使用 macOS launchd（推荐）

这是系统级定时任务，即使电脑重启也会自动运行，更可靠。

1. **安装依赖**（如果还没安装）：
   ```bash
   pip install schedule
   ```

2. **运行安装脚本**：
   ```bash
   ./install_scheduled_task.sh
   ```

3. **验证安装**：
   ```bash
   launchctl list | grep com.bilibili.quant.scheduled
   ```

4. **立即测试执行**（可选）：
   ```bash
   launchctl start com.bilibili.quant.scheduled
   ```

### 方式二：使用 Python schedule 库（需要保持进程运行）

这种方式需要保持 Python 进程一直运行。

1. **安装依赖**：
   ```bash
   pip install schedule
   ```

2. **运行定时任务脚本**：
   ```bash
   python3 scheduled_task.py
   ```

3. **测试执行**（立即执行一次）：
   ```bash
   python3 scheduled_task.py --once
   ```

4. **自定义执行时间**：
   ```bash
   python3 scheduled_task.py --time 15:30
   ```

## 任务内容

定时任务会在每天 15:30 自动执行以下操作：

1. **更新数据**：拉取今日最新数据并合并到缓存
2. **清理重复缓存**：删除重复的缓存文件
3. **执行回测**：运行策略回测，生成结果文件

## 管理命令（launchd 方式）

```bash
# 查看任务状态
launchctl list | grep com.bilibili.quant.scheduled

# 立即执行任务
launchctl start com.bilibili.quant.scheduled

# 停止任务（不会删除，只是暂停）
launchctl stop com.bilibili.quant.scheduled

# 卸载任务
launchctl unload ~/Library/LaunchAgents/com.bilibili.quant.scheduled.plist

# 重新加载任务（修改配置后）
launchctl load ~/Library/LaunchAgents/com.bilibili.quant.scheduled.plist
```

## 日志文件

- 标准输出：`logs/scheduled_task.log`
- 错误输出：`logs/scheduled_task_error.log`

## 注意事项

1. **确保 Python 环境正确**：定时任务会使用系统默认的 `python3`，确保已安装所需依赖
2. **网络连接**：确保执行时网络连接正常，能够访问数据源
3. **磁盘空间**：确保有足够的磁盘空间存储缓存和结果文件
4. **时间设置**：默认执行时间是 15:30（下午3:30），可以在 plist 文件中修改 `Hour` 和 `Minute` 字段

## 修改执行时间

### launchd 方式

编辑 `com.bilibili.quant.scheduled.plist` 文件，修改以下部分：

```xml
<key>StartCalendarInterval</key>
<dict>
    <key>Hour</key>
    <integer>15</integer>  <!-- 修改小时（0-23） -->
    <key>Minute</key>
    <integer>30</integer>  <!-- 修改分钟（0-59） -->
</dict>
```

然后重新加载：
```bash
launchctl unload ~/Library/LaunchAgents/com.bilibili.quant.scheduled.plist
launchctl load ~/Library/LaunchAgents/com.bilibili.quant.scheduled.plist
```

### Python schedule 方式

运行时指定时间：
```bash
python3 scheduled_task.py --time 15:30
```

或在代码中修改 `schedule.every().day.at("15:30")` 部分。
