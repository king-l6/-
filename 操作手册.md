# 操作手册

本文档说明项目中各个命令和脚本的功能及使用方法。

## 📋 目录

- [快速开始](#快速开始)
- [Web应用](#web应用)
- [批量回测](#批量回测)
- [每日任务](#每日任务)
- [补齐缓存并回测](#补齐缓存并回测)
- [每天下午5点拉取数据并跑六策略](#每天下午-5-点拉取数据--六策略回测)
- [定时任务](#定时任务)
- [API接口](#api接口)

---

## 🚀 快速开始

### 启动Web应用（推荐）

```bash
./run.sh
```

**功能**：
- 自动创建虚拟环境（如果不存在）
- 安装Python依赖
- 构建前端应用
- 启动Flask服务器

**访问地址**：http://localhost:8086

**停止服务**：按 `Ctrl+C`

---

## 🌐 Web应用

### 启动命令

```bash
python app.py
```

**功能**：
- 启动Flask Web服务器
- 提供策略回测Web界面
- 提供API接口

**环境变量**：
- `FLASK_PORT`：端口号（默认8086）
- `FLASK_ENV`：环境模式（development/production）

**示例**：
```bash
FLASK_PORT=8086 python app.py
```

---

## 📊 批量回测

### 命令

```bash
python batch_backtest.py
```

### 功能

批量执行多个策略回测，支持并行执行。

### 使用方法

**1. 执行所有策略**：
```bash
python batch_backtest.py
```

**2. 执行指定策略**：
```bash
python batch_backtest.py --strategies "龙头战法" "断板反包"
```

**3. 使用自定义策略文件**：
```bash
python batch_backtest.py --config custom_strategies.json
```

**4. 并行执行（默认）**：
```bash
python batch_backtest.py --parallel
```

**5. 串行执行**：
```bash
python batch_backtest.py --no-parallel
```

### 策略配置文件

默认使用 `common_strategies.json`，包含以下策略：
- 龙头战法
- 断板反包
- 均线上穿
- 情绪周期
- 三连板
- 筑底突破

### 输出结果

- 结果保存到 `results/` 目录
- 文件名格式：`{策略名称}_结果.jsonl`
- 显示汇总信息和每只股票的详细信息

---

## 📅 每日任务

### 命令

```bash
python daily_run.py
```

### 功能

执行完整的每日任务流程：
1. 清理重复缓存
2. 拉取今日数据并入已有缓存（只补缺失区间，增量）
3. 若缓存仍落后于最近交易日，再次增量补齐至最近交易日（不全量拉取 50 天）
4. 执行策略回测

### 执行流程

1. **数据更新**：
   - 检查本地缓存最新日期
   - 若落后于最近交易日，仅拉取「缓存最后日 + 1」到「最近交易日」的缺失区间并合并（增量，不全量）
   - 使用 100 并发补齐

2. **策略回测**：
   - 使用默认策略回测近30个交易日
   - 显示回测结果和股票涨跌情况

### 输出示例

```
[2026-02-10 15:30:00] 开始每日任务

步骤1: 拉取今日数据并入已有缓存
本地数据已是最新，跳过拉取

步骤2: 回测近一个月
开始回测策略...

回测完成！
1. 000001 平安银行 | 匹配日: 2026-02-05 | 匹配价: 12.50 | 现价: 12.80 | 涨跌: +2.40%
```

---

## 📥 补齐缓存并回测

### 适用场景

缓存中最后一天的数据是 **2月10日**，而最近交易日是 **2月24日** 时，需要把中间缺失的交易日（2月11日～2月24日）从数据源拉取到本地缓存，并执行常用策略回测。

### 命令（推荐）

在项目根目录执行：

```bash
python3 scripts/update_cache_and_backtest.py
# 若使用虚拟环境，请先激活：source venv/bin/activate，再用 python 或 python3
```

**功能**：
1. **步骤 1**：补齐缓存差值数据  
   - 读取本地缓存中每只股票的实际最后日期  
   - 若小于最近交易日，则拉取「最后日期 + 1 天」到「最近交易日」的数据并合并进对应缓存文件  
   - **只拉取缺失区间**，不重复拉取已有数据（增量补数，更快更省资源）  
2. **步骤 2**：执行常用策略回测  
   - 自动执行 `batch_backtest.py`，运行 `common_strategies.json` 中的全部策略（龙头战法、断板反包、均线上穿、情绪周期、三连板、筑底突破等）  
   - 回测前若某只股票缓存不足（如需 90 天历史），也**只拉取缺失的日期区间**与现有缓存合并，不会整段全量重拉  
   - 结果写入 `results/` 目录

### 可选参数

```bash
# 只补齐缓存，不执行回测
python3 scripts/update_cache_and_backtest.py --no-backtest

# 指定拉取数据时的并发数（默认 100）
python3 scripts/update_cache_and_backtest.py --workers 100

# 使用自定义策略配置文件
python3 scripts/update_cache_and_backtest.py --config my_strategies.json
```

### 与每日任务的区别

| 对比项     | 补齐缓存并回测                         | 每日任务 (daily_run.py)        |
|------------|----------------------------------------|--------------------------------|
| 数据拉取   | 只拉取「缓存最后日～最近交易日」的差值（增量） | 先增量补齐至最近交易日，不全量拉取 |
| 回测策略   | 常用策略（common_strategies.json）全部 | 固定一个默认策略、近 30 日     |
| 典型用途   | 缓存断档后一次性补全并跑常用策略       | 每天定时更新 + 简单回测        |

---

## 📆 每日增量回测（仅算「今天」一天）

当历史结果里**之前每一天都已经算过且不再变**时，可以每天只以 **T = 今天**（或指定日期）扫描一次，输出按天分文件的结果，更高效。

### 逻辑说明

- **前提**：缓存最新交易日 ≥ 昨天（建议先跑 `update_cache_and_backtest.py` 补齐到最新）。
- **本次**：只计算 **match_date = 指定 T 日** 的信号，不重算历史。
- **输出**：每个策略一个按日文件：`results/{策略名}_{YYYYMMDD}_结果.jsonl`，例如 `龙头战法_20260226_结果.jsonl`。
- **历史页**：前端「历史结果」会按策略聚合展示（主文件 + 所有按日文件合并，按 match_date 排序）。

### 命令

在项目根目录执行：

```bash
# 以「最近交易日」为 T 日（通常为今天或昨天）
python3 scripts/incremental_backtest.py

# 指定 T 日
python3 scripts/incremental_backtest.py --date 2026-02-26

# 不检查缓存是否最新，直接跑（缓存落后时也可跑）
python3 scripts/incremental_backtest.py --no-check-cache

# 指定并发数（默认 50）
python3 scripts/incremental_backtest.py --workers 50
```

### 输出目录与文件

- 目录：`results/`
- 文件名：`{策略名}_{YYYYMMDD}_结果.jsonl`（如 `龙头战法_20260226_结果.jsonl`）
- 与全量回测的 `{策略名}_结果.jsonl` 并存；历史页面会自动合并同一策略的「主文件 + 按日文件」按日期展示。

### 与全量回测的配合

| 场景           | 建议做法 |
|----------------|----------|
| 首次或断档很久 | 先跑 `update_cache_and_backtest.py` 做一次全量回测 |
| 之后每天       | 跑 `incremental_backtest.py`，只算当天 T 日，写入按日文件 |
| 历史结果查看   | 打开「历史结果」页，选策略名「xxx (按日期聚合)」即可看到按 match_date 聚合的完整结果 |

---

## 🕔 每天下午 5 点：拉取数据 + 六策略回测

适用于**每天固定时间**（如收盘后 17:00）自动：补齐缓存数据 + 跑齐六个常用策略（龙头战法、断板反包、均线上穿、情绪周期、三连板、筑底突破），结果写入 `results/`。

### 方式一：立即执行一次

在项目根目录执行（拉取数据 + 六策略回测）：

```bash
python3 scripts/update_cache_and_backtest.py
# 或
python3 scripts/daily_backtest_17h.py
python3 scripts/daily_backtest_17h.py --now
```

可选：指定拉取并发数 `--workers 10`。

### 方式二：cron 每天 17:00 自动执行（推荐）

1. 创建日志目录（可选）：`mkdir -p logs`
2. 编辑 crontab：`crontab -e`
3. 添加一行（把 `/path/to/量化` 换成实际项目根目录）：

```bash
0 17 * * * cd /path/to/量化 && python3 scripts/update_cache_and_backtest.py --workers 100 >> /path/to/量化/logs/daily_backtest.log 2>&1
```

或使用 shell 脚本（需可执行权限 `chmod +x scripts/run_daily_5pm.sh`）：

```bash
0 17 * * * /path/to/量化/scripts/run_daily_5pm.sh >> /path/to/量化/logs/daily_backtest.log 2>&1
```

### 方式三：Python 定时（常驻进程，每天 17:00）

需要进程一直运行，到点自动执行：

```bash
python scripts/daily_backtest_17h.py --schedule
```

按 `Ctrl+C` 可退出。依赖已包含在 `requirements.txt` 的 `schedule`。

```bash
python3 scripts/daily_backtest_17h.py --schedule
```

---

## ⏰ 定时任务

### 方式一：macOS launchd（推荐）

**安装定时任务**：
```bash
./install_scheduled_task.sh
```

**功能**：
- 自动安装到macOS launchd系统
- 每天15:30自动执行每日任务
- 日志保存到 `logs/` 目录

**管理命令**：
```bash
# 查看任务状态
launchctl list | grep com.bilibili.quant.scheduled

# 立即执行一次
launchctl start com.bilibili.quant.scheduled

# 停止任务
launchctl stop com.bilibili.quant.scheduled

# 卸载任务
launchctl unload ~/Library/LaunchAgents/com.bilibili.quant.scheduled.plist
```

### 方式二：Python schedule（需保持进程运行）

**启动定时任务**：
```bash
python scheduled_task.py
```

**立即执行一次（测试）**：
```bash
python scheduled_task.py --once
```

**自定义执行时间**：
```bash
python scheduled_task.py --time 16:00
```

**功能**：
- 使用Python schedule库定时执行
- 默认每天15:30执行
- 需要保持进程运行

---

## 🔌 API接口

### 1. 策略回测

**接口**：`POST /api/backtest`

**请求体**：
```json
{
  "strategy": {
    "conditions": [
      {"type": "limit_up", "date1": -2},
      {"type": "pct_change_gt", "date1": 0, "value": 0}
    ],
    "exclude": {
      "kcb": true,
      "cyb": true,
      "bjs": true,
      "st": true,
      "delist": true
    },
    "timeRange": 30
  },
  "strategy_name": "自定义策略"
}
```

**响应**：
```json
{
  "success": true,
  "data": [
    {
      "code": "000001",
      "name": "平安银行",
      "match_date": "2026-02-05",
      "match_price": 12.50,
      "current_price": 12.80
    }
  ],
  "count": 1
}
```

### 2. 批量回测

**接口**：`POST /api/backtest/batch`

**请求体**：
```json
{
  "strategies": [
    {
      "conditions": [...],
      "timeRange": 30
    }
  ],
  "strategy_names": ["策略1", "策略2"]
}
```

### 3. 获取股票列表

**接口**：`GET /api/stocks`

**响应**：
```json
{
  "success": true,
  "data": [
    {"code": "000001", "name": "平安银行"},
    {"code": "600000", "name": "浦发银行"}
  ]
}
```

### 4. 获取结果文件列表

**接口**：`GET /api/results/list`

**响应**：
```json
{
  "success": true,
  "data": [
    {
      "filename": "龙头战法_结果.jsonl",
      "size": 1024,
      "modified": "2026-02-10T15:30:00"
    }
  ]
}
```

### 5. 获取结果文件内容

**接口**：`GET /api/results/file?filename=龙头战法_结果.jsonl`

**响应**：
```json
{
  "success": true,
  "data": {
    "meta": {
      "strategy_name": "龙头战法",
      "run_at": "2026-02-10T15:30:00",
      "count": 10
    },
    "results": [...],
    "count": 10
  }
}
```

---

## 📁 项目结构

```
量化/
├── app.py                    # Flask Web应用
├── data_fetcher.py           # 数据获取模块
├── strategy_engine.py        # 策略回测引擎
├── batch_backtest.py         # 批量回测脚本
├── daily_run.py              # 每日任务脚本
├── scheduled_task.py         # 定时任务脚本
├── common_strategies.json    # 策略配置文件
├── run.sh                    # 启动脚本
├── install_scheduled_task.sh  # 安装定时任务脚本
├── com.bilibili.quant.scheduled.plist  # macOS定时任务配置
├── cache/                    # 数据缓存目录
│   ├── stock_list.json       # 股票列表缓存
│   └── stock_data/           # 各股K线缓存
├── results/                  # 回测结果目录
├── frontend/                 # 前端源码
└── static/                   # 前端构建产物
```

---

## ⚙️ 环境配置

### Python环境

**macOS 提示**：若使用系统自带的 Python（如 Homebrew 安装），会报 `externally-managed-environment`，不能直接用 `pip3 install`。请使用项目虚拟环境。

**创建并激活虚拟环境**：
```bash
# 在项目根目录执行
python3 -m venv venv
source venv/bin/activate   # macOS/Linux；激活后终端前会显示 (venv)
```

**安装依赖**（在激活 venv 后）：
```bash
pip install -r requirements.txt
```

之后运行脚本、启动应用等都在**已激活 venv 的终端**里执行，例如：
```bash
source venv/bin/activate
python scripts/update_cache_and_backtest.py
# 或
./run.sh   # 会自动创建/激活 venv 并安装依赖
```

### 环境变量（.env文件）

可选配置项：
- `FLASK_PORT`：Flask端口（默认8086）
- `FLASK_ENV`：环境模式（development/production）

---

## 📝 注意事项

1. **数据源**：使用 **Baostock** 免费接口，日 K 线数据一般在**当日 17:30** 后更新；若需回测包含当天，建议 17:30 之后或次日再跑拉数/回测。详见 [DATA_SOURCE.md](DATA_SOURCE.md)。
2. **首次运行**：需要下载股票列表和历史数据，可能需要一些时间
2. **数据缓存**：数据会自动缓存到 `cache/` 目录，提高后续运行速度
3. **回测时间**：回测过程会遍历所有股票，可能需要几分钟时间
4. **网络要求**：需要网络连接获取股票数据
5. **并发控制**：默认使用50个并发线程，可根据机器性能调整

---

## 🔧 常见问题

**Q: 如何修改回测并发数？**

A: 修改 `app.py` 中的 `max_workers` 参数：
```python
strategy_engine = StrategyEngine(data_fetcher, max_workers=50)
```

**Q: 如何修改定时任务执行时间？**

A: 编辑 `com.bilibili.quant.scheduled.plist` 文件中的 `Hour` 和 `Minute` 字段，然后重新加载：
```bash
launchctl unload ~/Library/LaunchAgents/com.bilibili.quant.scheduled.plist
launchctl load ~/Library/LaunchAgents/com.bilibili.quant.scheduled.plist
```

**Q: 回测结果保存在哪里？**

A: 结果保存在 `results/` 目录，文件格式为 `{策略名称}_结果.jsonl`

**Q: 如何查看回测结果？**

A: 可以通过Web界面查看，或直接读取 `results/` 目录下的 `.jsonl` 文件
