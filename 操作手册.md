# A股策略回测系统 - 操作手册

本文档汇总项目所有操作、脚本及使用方式，便于快速查阅与执行。

---

## 一、快速入口

| 操作目的 | 推荐命令 |
|----------|----------|
| 启动 Web 界面 | `./run.sh` 或 `python app.py` |
| 仅更新今日数据 | `python fetch_today.py` |
| 拉取数据并回测 | `python fetch_and_backtest.py` |
| 仅执行回测 | `python run_backtest.py` |
| 检查任务进度 | `python check_progress.py` |
| 立即执行每日任务 | `python scheduled_task.py --once` |

---

## 二、安装与启动

### 2.1 环境准备

```bash
# 创建虚拟环境（推荐）
python3 -m venv venv
source venv/bin/activate   # macOS/Linux
# 或 venv\Scripts\activate  # Windows

# 安装依赖
pip install -r requirements.txt
```

### 2.2 启动 Web 应用

```bash
./run.sh
# 或
python app.py
```

- 访问地址：http://localhost:8086
- 按 Ctrl+C 停止服务

### 2.3 加载环境变量（可选）

若项目根目录存在 `.env` 文件，`run.sh` 和部分脚本会自动加载。可用于配置邮件、代理等。

---

## 三、数据操作

### 3.1 仅拉取今日数据

**脚本**：`fetch_today.py`

**功能**：拉取最近交易日数据，合并到已有缓存 JSON 文件。

```bash
python fetch_today.py
```

**执行流程**：
1. 清理重复缓存（`remove_duplicate_cache`）
2. 拉取今日数据并入缓存（`update_caches_with_today_data`）

---

### 3.2 批量获取近一个月数据

**脚本**：`fetch_all_stocks.py`

**功能**：批量获取所有主板股票近 30 天数据，写入 `cache/stock_data/`。

```bash
python fetch_all_stocks.py
```

**参数**：
- 日期范围：近 30 日历日
- 并发：10 线程
- 使用缓存：优先使用已有缓存

---

### 3.3 拉取数据 + 执行回测

**脚本**：`fetch_and_backtest.py`

**功能**：拉取近一个月数据、更新到最新交易日，再执行策略回测。

```bash
python fetch_and_backtest.py
```

**执行流程**：
1. 拉取最近约 42 天数据（覆盖约 30 个交易日）
2. 调用 `update_caches_with_today_data` 更新到最新
3. 执行回测（按 `新策略说明.md` 中的策略）

---

### 3.4 数据相关核心 API（data_fetcher.py）

| 方法 | 说明 |
|------|------|
| `get_stock_list()` | 获取主板股票列表（00/60 开头，排除 ST、退市） |
| `get_stock_data(code, start_date, end_date)` | 获取单只股票 K 线，优先读缓存 |
| `remove_duplicate_cache()` | 删除重复缓存，每只股票只保留一份 |
| `update_caches_with_today_data(max_workers)` | 拉取今日数据并入已有缓存 |
| `need_fetch_recent_data()` | 判断缓存是否落后于最近交易日 |
| `get_local_cache_latest_date()` | 获取本地缓存最新日期 |

**数据源**：Baostock（免费 A 股接口，详见 `DATA_SOURCE.md`）

---

## 四、回测操作

### 4.1 命令行回测

**脚本**：`run_backtest.py`

**功能**：使用已有缓存数据执行策略回测，不拉取新数据。

```bash
python run_backtest.py
```

**策略配置**：按 `新策略说明.md` 默认策略（T-5 涨停、T-4/T-3 涨幅、成交量比例等）。

---

### 4.2 Web 界面回测

1. 启动应用：`./run.sh`
2. 打开 http://localhost:8086
3. 点击「加载示例策略」填充条件
4. 点击「开始回测」

**API**：

- `POST /api/backtest`：提交策略并执行回测
- `GET /api/stocks`：获取股票列表

---

### 4.3 回测结果

- 保存路径：`results/策略_YYYYMMDD_HHMMSS_结果.jsonl`
- 每条结果实时追加，包含：`code`、`name`、`match_date`、`match_price`、`current_price` 等

---

## 五、每日任务

### 5.1 每日任务脚本

**脚本**：`daily_run.py`

**功能**：完整每日流程：更新数据 + 回测。

```bash
python daily_run.py
```

**执行流程**：
1. 清理重复缓存
2. 拉取股票列表
3. 拉取今日数据并入缓存（`update_caches_with_today_data`）
4. 若缓存落后，拉取近 50 天数据补充
5. 执行回测并输出结果

---

## 六、定时任务

### 6.1 使用 macOS launchd（推荐）

**安装**：

```bash
./install_scheduled_task.sh
```

**管理**：

```bash
# 查看任务状态
launchctl list | grep com.bilibili.quant.scheduled

# 立即执行一次
launchctl start com.bilibili.quant.scheduled

# 停止任务
launchctl stop com.bilibili.quant.scheduled

# 卸载任务
launchctl unload ~/Library/LaunchAgents/com.bilibili.quant.scheduled.plist

# 修改配置后重新加载
launchctl load ~/Library/LaunchAgents/com.bilibili.quant.scheduled.plist
```

**默认执行时间**：每天 15:30

**日志**：`logs/scheduled_task.log`、`logs/scheduled_task_error.log`

---

### 6.2 使用 Python schedule（需保持进程运行）

```bash
# 定时执行（默认每天 15:30）
python scheduled_task.py

# 自定义时间
python scheduled_task.py --time 16:00

# 立即执行一次（测试）
python scheduled_task.py --once
```

---

## 七、进度检查

**脚本**：`check_progress.py`

**功能**：检查任务运行状态、缓存进度、数据新鲜度、最新回测结果。

```bash
python check_progress.py
```

**输出示例**：
- 任务是否在运行（`daily_run.py`）
- 缓存文件统计（已有/缺少数量）
- 数据是否最新（最近交易日 vs 缓存最新日期）
- 最新回测结果文件及前 5 只股票

---

## 八、邮件发送

**脚本**：`send_email.py`

**功能**：通过 SMTP 发送邮件，支持正文与附件，由环境变量配置。

**必填环境变量**：
- `SMTP_HOST`：SMTP 服务器
- `SMTP_USER`：用户名
- `SMTP_PASS`：密码/授权码
- `SMTP_FROM`：发件人
- `SMTP_TO`：收件人（多个用逗号分隔）

**可选环境变量**：
- `SMTP_PORT`：端口，默认 465
- `SMTP_SUBJECT`：主题
- `SMTP_BODY`：正文
- `SMTP_BODY_FILE`：正文文件路径
- `SMTP_ATTACH`：附件路径
- `SMTP_USE_TLS`：是否使用 STARTTLS

```bash
export SMTP_HOST=smtp.example.com
export SMTP_USER=user
export SMTP_PASS=pass
export SMTP_FROM=from@example.com
export SMTP_TO=to@example.com
export SMTP_ATTACH=results/策略_20260205_结果.jsonl
python send_email.py
```

---

## 九、目录结构

```
量化/
├── app.py                    # Flask Web 应用
├── data_fetcher.py           # 数据获取模块
├── strategy_engine.py        # 策略回测引擎
├── daily_run.py              # 每日任务入口
├── scheduled_task.py         # 定时任务
├── fetch_today.py            # 仅拉今日数据
├── fetch_all_stocks.py       # 批量拉近一个月数据
├── fetch_and_backtest.py     # 拉数据 + 回测
├── run_backtest.py           # 仅回测
├── check_progress.py         # 进度检查
├── send_email.py             # 邮件发送
├── run.sh                    # 启动脚本
├── install_scheduled_task.sh # 安装 launchd 定时任务
├── com.bilibili.quant.scheduled.plist  # launchd 配置
├── cache/                    # 数据缓存
│   ├── stock_list.json       # 股票列表缓存
│   └── stock_data/           # 各股 K 线缓存
├── results/                  # 回测结果（.jsonl）
├── logs/                     # 定时任务日志
├── templates/                # 前端模板
└── static/                   # 静态资源
```

---

## 十、相关文档

| 文档 | 说明 |
|------|------|
| `README.md` | 项目简介与快速开始 |
| `新策略说明.md` | 策略条件与使用方法 |
| `DATA_SOURCE.md` | 数据源说明（Baostock） |
| `定时任务说明.md` | 定时任务安装与管理 |
| `docs/前端设计.md` | 前端规范 |
| `docs/后端规范.md` | 后端规范 |

---

## 十一、常见问题

**Q: 回测结果为空？**
- 策略条件过严
- 时间范围内无符合条件股票
- 数据获取失败（检查网络）

**Q: 如何提高回测速度？**
- 减小回测时间范围（timeRange）
- 减少条件数量
- 确保网络稳定、缓存有效

**Q: 代理导致请求失败？**
- 脚本已设置 `NO_PROXY='*'` 直连
- 若仍有问题，检查系统/终端代理配置

**Q: 修改定时任务执行时间？**
- launchd：编辑 `com.bilibili.quant.scheduled.plist` 中 `Hour`、`Minute`，再重新 load
- schedule：`python scheduled_task.py --time 16:00`
